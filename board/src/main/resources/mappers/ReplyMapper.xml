<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.board.mapper.ReplyMapper">
	<insert id="insert">
		INSERT INTO TABLE_REPLY(RNO, BNO, REPLY, REPLIER)
		VALUES(SEQUENCE_REPLY.NEXTVAL, #{bno}, #{reply}, #{replier} )
	</insert>
	
	<select id="select" resultType="replyVO">
		SELECT RNO, BNO, REPLY, REPLIER, REPLYDATE, UPDATEDATE FROM TABLE_REPLY
		WHERE RNO = #{rno}
	</select>
</mapper>

